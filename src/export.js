export function downloadCSV(rows,name){if(!rows?.length)return;const keys=Object.keys(rows[0]),esc=v=>`"${String(v??"").replaceAll('"','""')}"`,csv="\ufeff"+keys.join(";")+"\n"+rows.map(r=>keys.map(k=>esc(r[k])).join(";")).join("\n"),a=document.createElement("a");a.href=URL.createObjectURL(new Blob([csv],{type:"text/csv;charset=utf-8"}));a.download=name;a.click()}export function printReport(p,c,issues){const html=`<!doctype html><html><head><meta charset="utf-8"><title>${p.name} — отчёт</title><style>body{font:14px Arial;margin:32px}table{width:100%;border-collapse:collapse}td,th{border:1px solid #bbb;padding:6px;text-align:left}</style></head><body><h1>Электромонтаж дома PRO</h1><h2>${p.name}</h2><p>Площадь: ${c.area.toFixed(1)} м² • Установленная: ${c.installed.toFixed(2)} кВт • Расчётная: ${c.demand.toFixed(2)} кВт</p><h3>Материалы и смета</h3><table><tr><th>Категория</th><th>Материал</th><th>Количество</th><th>Цена</th><th>Сумма</th></tr>${c.materialRows.map(r=>`<tr><td>${r.category}</td><td>${r.name}</td><td>${r.qty} ${r.unit}</td><td>${r.price} ₸</td><td>${r.sum} ₸</td></tr>`).join("")}</table><h3>Проверка</h3>${issues.map(i=>`<p>${i.level==='error'?'✖':i.level==='warn'?'⚠':'✓'} ${i.text}</p>`).join("")}<p><b>Итого материалов: ${c.materialTotal.toLocaleString()} ₸</b></p><p>Расчёт является предварительным. Перед монтажом необходимо проверить проект и соответствие действующим нормам электробезопасности.</p><script>window.onload=()=>window.print()</script></body></html>`;const w=window.open();w.document.write(html);w.document.close()}